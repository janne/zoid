Overview
--------

This app is a CLI app, able to run in daemon mode.

It's written in zig.

Keep all code, commit messages and copy in English, even if prompted in other languages.

## Rules
- Keep the tests updated.
- After code updates:
  - Format the code
  - Run tests

